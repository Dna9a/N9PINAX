# PFE Cybersécurité — Network Security Scanner
# Web / dashboard (FastAPI) dependencies.
# Install with: pip install -r requirements-web.txt
#
# NOTE: This project's API is FastAPI + SSE only. The old Flask stack
# (flask / flask-login / flask-limiter / flask-socketio) was removed — it was
# unused and pulled in conflicting transitive deps (python-socketio, engineio,
# limits, werkzeug, …).

bcrypt>=4.1.0              # Secure password hashing
fastapi>=0.111.0          # Modern, high-performance web framework
uvicorn[standard]>=0.34.0 # ASGI server for FastAPI (+ websockets/uvloop/httptools)
sse-starlette>=2.0.0      # Server-Sent Events helper for FastAPI/Starlette
aiosqlite>=0.19.0         # Async SQLite driver (optional, future-proof persistence)
redis[hiredis]>=5.0.0     # Cache + cross-worker rate-limit / pub-sub (hiredis = faster I/O)
